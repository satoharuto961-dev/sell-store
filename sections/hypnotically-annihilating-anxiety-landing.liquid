{% assign product = all_products['hypnotically-annihilating-anxiety-penetrating-confessions-of-a-rogue-hypnotist'] %}
{% assign primary_variant = product.selected_or_first_available_variant %}
{% assign hero_image = 'https://cdn.shopify.com/s/files/1/0989/7020/6389/files/519V7AX-JoL.jpg?v=1765713971' %}

<section class="hypno-landing">
  <style>
    .hypno-landing {
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      background: radial-gradient(circle at 12% 20%, #e0f2fe 0, #f8fafc 26%, transparent 32%),
                  radial-gradient(circle at 88% 10%, #fee2e2 0, #fff5f5 20%, transparent 32%),
                  linear-gradient(180deg, #ffffff 0%, #f1f5f9 100%);
      color: #0f172a;
      padding: 56px 0 120px;
    }

    .hypno-landing .container {
      max-width: 1160px;
      margin: 0 auto;
      padding: 0 20px;
    }

    .hypno-landing .hero {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 26px;
      align-items: center;
      background: #ffffff;
      border-radius: 18px;
      padding: 30px;
      border: 1px solid #e2e8f0;
      box-shadow: 0 16px 44px rgba(15, 23, 42, 0.08);
    }

    .hypno-landing .eyebrow {
      display: inline-flex;
      gap: 10px;
      align-items: center;
      background: #0f172a;
      color: #e2e8f0;
      padding: 8px 12px;
      border-radius: 999px;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      font-weight: 800;
      font-size: 0.82rem;
    }

    .hypno-landing .eyebrow .pulse {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: #22c55e;
      box-shadow: 0 0 0 6px rgba(34, 197, 94, 0.18);
    }

    .hypno-landing h1 {
      font-size: clamp(2.1rem, 3.6vw, 2.9rem);
      margin: 14px 0 12px;
      line-height: 1.15;
    }

    .hypno-landing .subtitle {
      color: #334155;
      font-size: 1.05rem;
      line-height: 1.7;
      margin-bottom: 16px;
    }

    .hypno-landing .pill {
      display: inline-block;
      background: #dbeafe;
      color: #1d4ed8;
      padding: 6px 12px;
      border-radius: 999px;
      font-weight: 700;
      font-size: 0.95rem;
      margin-bottom: 8px;
    }

    .hypno-landing .bullets {
      list-style: none;
      padding: 0;
      margin: 16px 0;
      display: grid;
      gap: 10px;
    }

    .hypno-landing .bullets li {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 10px;
      align-items: start;
      padding: 10px 12px;
      background: #f8fafc;
      border-radius: 12px;
      border: 1px solid #e2e8f0;
      font-weight: 600;
      color: #0f172a;
    }

    .hypno-landing .bullets svg {
      width: 16px;
      height: 16px;
      color: #16a34a;
      margin-top: 2px;
    }

    .hypno-landing .media {
      background: linear-gradient(180deg, #ffffff 0%, #e2e8f0 100%);
      border-radius: 16px;
      padding: 16px;
      border: 1px solid #e2e8f0;
      box-shadow: 0 12px 30px rgba(15, 23, 42, 0.08);
    }

    .hypno-landing .media img {
      display: block;
      width: 100%;
      height: auto;
      border-radius: 12px;
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.92);
    }

    .hypno-landing .cta-card {
      margin-top: 18px;
      background: #0f172a;
      color: #fff;
      padding: 18px;
      border-radius: 14px;
      display: grid;
      gap: 12px;
      box-shadow: 0 16px 34px rgba(15, 23, 42, 0.28);
    }

    .hypno-landing .price-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      align-items: baseline;
    }

    .hypno-landing .price {
      font-size: 1.7rem;
      font-weight: 900;
    }

    .hypno-landing .label {
      font-size: 0.98rem;
      color: #cbd5e1;
    }

    .hypno-landing .cta-card button {
      background: linear-gradient(135deg, #10b981, #22c55e);
      border: none;
      color: #0f172a;
      font-weight: 800;
      padding: 12px 14px;
      border-radius: 10px;
      cursor: pointer;
      width: 100%;
      font-size: 1rem;
      box-shadow: 0 12px 28px rgba(16, 185, 129, 0.28);
      transition: transform 0.1s ease, box-shadow 0.2s ease;
    }

    .hypno-landing .cta-card button:hover,
    .hypno-landing .cta-card button:focus {
      transform: translateY(-1px);
      box-shadow: 0 16px 32px rgba(16, 185, 129, 0.35);
    }

    .hypno-landing .cta-card button[disabled] {
      opacity: 0.6;
      cursor: not-allowed;
      transform: none;
      box-shadow: none;
    }

    .hypno-landing .status {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-weight: 700;
    }

    .hypno-landing .status .dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: #22c55e;
      box-shadow: 0 0 0 5px rgba(34, 197, 94, 0.18);
    }

    .hypno-landing .reassurance {
      color: #e2e8f0;
      font-size: 0.95rem;
      margin: 0;
    }

    .hypno-landing .feature-grid {
      margin-top: 40px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 16px;
    }

    .hypno-landing .feature-card {
      background: #fff;
      border: 1px solid #e2e8f0;
      border-radius: 14px;
      padding: 16px;
      box-shadow: 0 12px 30px rgba(15, 23, 42, 0.06);
    }

    .hypno-landing .feature-card h3 {
      margin: 0 0 6px;
      font-size: 1.04rem;
    }

    .hypno-landing .feature-card p {
      margin: 0;
      color: #334155;
      line-height: 1.6;
      font-size: 0.98rem;
    }

    .hypno-landing .highlight-banner {
      margin-top: 36px;
      background: linear-gradient(135deg, #0f172a, #111827);
      color: #e2e8f0;
      border-radius: 16px;
      padding: 18px;
      display: grid;
      gap: 10px;
      box-shadow: 0 16px 40px rgba(15, 23, 42, 0.26);
    }

    .hypno-landing .highlight-banner strong {
      color: #22c55e;
    }

    .hypno-landing .list-title {
      margin: 50px 0 12px;
      font-size: clamp(1.5rem, 2.8vw, 2rem);
    }

    .hypno-landing .chapter-list {
      display: grid;
      gap: 10px;
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .hypno-landing .chapter-list li {
      background: #fff;
      border: 1px solid #e2e8f0;
      padding: 12px 14px;
      border-radius: 12px;
      display: flex;
      gap: 10px;
      align-items: center;
      font-weight: 700;
      color: #0f172a;
    }

    .hypno-landing .chapter-list li::before {
      content: "";
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: #22c55e;
      box-shadow: 0 0 0 5px rgba(34, 197, 94, 0.15);
    }

    .hypno-landing .grid-two {
      margin-top: 32px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 16px;
      align-items: start;
    }

    @media (max-width: 720px) {
      .hypno-landing { padding-bottom: 80px; }
      .hypno-landing .hero { padding: 22px; }
    }
  </style>

  <div class="container">
    <div class="hero">
      <div class="copy">
        <span class="eyebrow"><span class="pulse"></span>Digital Playbook</span>
        <h1>Hypnotically Annihilating Anxiety: Penetrating Confessions of a Rogue Hypnotist</h1>
        <p class="subtitle">Step inside the methods a renegade hypnotist uses to disarm panic, rewire anxious patterns, and rebuild inner calm in real-world sessions.</p>
        <span class="pill">Instant digital download · $29</span>
        <ul class="bullets">
          <li>
            <svg viewBox="0 0 16 16" fill="currentColor" aria-hidden="true"><path d="M6.173 12.927 1.4 8.154l1.4-1.4 3.373 3.373 7.027-7.027 1.4 1.4z"/></svg>
            Plain-language walkthroughs of hypnotic scripts that neutralize anxiety spikes.
          </li>
          <li>
            <svg viewBox="0 0 16 16" fill="currentColor" aria-hidden="true"><path d="M6.173 12.927 1.4 8.154l1.4-1.4 3.373 3.373 7.027-7.027 1.4 1.4z"/></svg>
            Field notes from clients who moved from spiraling thoughts to grounded control.
          </li>
          <li>
            <svg viewBox="0 0 16 16" fill="currentColor" aria-hidden="true"><path d="M6.173 12.927 1.4 8.154l1.4-1.4 3.373 3.373 7.027-7.027 1.4 1.4z"/></svg>
            Quick resets, reframes, and hypnotic anchors you can use within minutes.
          </li>
        </ul>

        <div class="cta-card">
          <div class="price-row">
            <span class="price">{{ primary_variant.price | money }}</span>
            <span class="label">One-time purchase · Digital product</span>
          </div>
          {% form 'product', product, class: 'cta-form' %}
            <input type="hidden" name="id" value="{{ primary_variant.id }}">
            <button type="submit" {% unless primary_variant.available %}disabled{% endunless %}>
              {% if primary_variant.available %}Get the download{% else %}Sold out{% endif %}
            </button>
          {% endform %}
          <div class="status"><span class="dot"></span>Secure checkout</div>
          <p class="reassurance">PDF + mobile-friendly formats. Instant access after purchase.</p>
        </div>
      </div>
      <div class="media">
        <img src="{{ hero_image }}" alt="Hypnotically Annihilating Anxiety cover">
      </div>
    </div>

    <div class="feature-grid">
      <div class="feature-card">
        <h3>Calm in minutes</h3>
        <p>Micro-hypnosis scripts designed to interrupt spirals and activate calm breathing and focus fast.</p>
      </div>
      <div class="feature-card">
        <h3>Session snapshots</h3>
        <p>Real transcripts annotated with why each suggestion works and how to adapt it to your own triggers.</p>
      </div>
      <div class="feature-card">
        <h3>Build lasting resilience</h3>
        <p>Daily conditioning routines, reframes, and self-suggestions to strengthen your baseline state.</p>
      </div>
      <div class="feature-card">
        <h3>Works anywhere</h3>
        <p>No props or prep required—just proven language patterns that fit into commutes, meetings, or bedtime.</p>
      </div>
    </div>

    <div class="highlight-banner">
      <div class="status"><span class="dot"></span> No shipping—your files are ready the moment you check out.</div>
      <p class="reassurance">Includes lifetime updates, printable pages, and a concise quick-start guide so you can begin a 5-minute calming loop tonight.</p>
    </div>

    <h2 class="list-title">Inside the rogue hypnotist's playbook</h2>
    <div class="grid-two">
      <ul class="chapter-list">
        <li>Rapid grounding inductions for panic peaks</li>
        <li>Language pivots that dissolve catastrophic thinking</li>
        <li>Confidence anchors you can trigger on demand</li>
        <li>Nighttime scripts for deeper, more restorative sleep</li>
        <li>Post-anxiety decompression rituals to reset your nervous system</li>
      </ul>
      <ul class="chapter-list">
        <li>Reframing loops for intrusive thoughts</li>
        <li>Self-hypnosis templates for public speaking and performance</li>
        <li>Guided visualizations to rewire anxious memories</li>
        <li>Client case studies with exact wording and timing notes</li>
        <li>Downloadable checklists and prompts for daily practice</li>
      </ul>
    </div>
  </div>
</section>
